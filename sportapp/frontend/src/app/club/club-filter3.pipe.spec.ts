import { ClubFilter3Pipe } from './club-filter3.pipe';

describe('ClubFilter3Pipe', () => {
  it('create an instance', () => {
    const pipe = new ClubFilter3Pipe();
    expect(pipe).toBeTruthy();
  });
});

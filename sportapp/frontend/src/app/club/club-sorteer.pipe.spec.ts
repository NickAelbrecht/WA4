import { ClubSorteerPipe } from './club-sorteer.pipe';

describe('ClubSorteerPipe', () => {
  it('create an instance', () => {
    const pipe = new ClubSorteerPipe();
    expect(pipe).toBeTruthy();
  });
});

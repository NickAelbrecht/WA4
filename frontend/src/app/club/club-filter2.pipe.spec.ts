import { ClubFilter2Pipe } from './club-filter2.pipe';

describe('ClubFilter2Pipe', () => {
  it('create an instance', () => {
    const pipe = new ClubFilter2Pipe();
    expect(pipe).toBeTruthy();
  });
});
